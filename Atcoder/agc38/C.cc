#include <bits/stdc++.h>

using namespace std;

void solve() {
    int N; cin >> N;
    vector<int> nums(N);
    for (int i = 0; i < N; ++i) cin >> nums[i];
    
}

int main() {
    int n; n = 1;
    while (n--) {
        solve();
    }
}